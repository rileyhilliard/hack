# Default Configuration for WebAI Proxy

# Backend AI Service Configuration
TARGET_DOMAIN=localhost
TARGET_PORT=10501

# Proxy Server Configuration
PROXY_DOMAIN=localhost
PROXY_PORT=8080

# Optional: API Key for the backend service
# The proxy will prioritize the 'Authorization: Bearer <token>' header from incoming requests.
# If no Authorization header is present, this TARGET_API_KEY will be sent to the backend
# as the 'X-API-Key' header.
# TARGET_API_KEY=

# Optional: Timeout in milliseconds to wait for backend server response (default: 120000 = 2 minutes)
TAR#ET_TIMEOUT_MS=120000